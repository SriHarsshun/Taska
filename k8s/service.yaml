apiVersion: v1
kind: Service
metadata:
  name: taska-service
  labels:
    app: taska
spec:
  type: NodePort
  selector:
    app: taska
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30000
